#!/bin/bash

echo "Preparing the 'Tester' micro-framework for the installation" && \
  git submodule init && git submodule update && \
  echo "The 'Tester' micro-framework is prepared for the installation"